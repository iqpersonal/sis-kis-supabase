rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users to read all SiS collections
    match /{collection}/{docId} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
